---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by ligs.
--- DateTime: 2020-08-06 21:17
---

local BRIDGE = require 'ejoysdk_lua.ejoysdk_js_bridge_adapter'
local M = {}

local function callback(cb, s, b)
    if cb then
        cb({ succ = s, body = b or {}, ver = BRIDGE.VER.V2 })
    end
end

function M.succ_callback(cb, body)
    callback(cb, true, body)
end

function M.fail_callback(cb, ec, em, ei)
    callback(cb, false, { err_code = ec or -1, err_msg = em, ext_info = ei or {} })
end

function M.make_succ_resp(body)
    return {succ = true, body = body or {}, ver = BRIDGE.VER.V2 }
end

function M.make_fail_resp(ec, em, ei)
    return {succ = false, body = {err_code = ec or -1, err_msg = em, ext_info = ei or {} }}
end

return M
